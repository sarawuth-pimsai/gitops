name: On Pull Request Main
on:
  pull_request:
    branches: [main]
jobs:
  print_event_env:
    runs-on: [self-hosted]
    steps:
      - run: echo '${{ toJSON(github.event.pull_request) }}'
  use_rebase: 
    runs-on: [self-hosted]
    - uses: sarawuth-pimsai/gitops/.github/actions/rebase
      